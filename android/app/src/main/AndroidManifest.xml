<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <!--
          طبقة أمان (خصوصية): لو سحبت أي تبعية غير مباشرة حزمة Firebase/Google
          Analytics، تمنعها هذه الأعلام من تشغيل التحليلات أو جمع المعرّف الإعلاني.
          النتيجة: المعرّف الإعلاني يُستخدم حصراً عبر AdMob لعرض الإعلانات — وهو ما
          يطابق تصريح Data Safety و"المعرّفات الإعلانية" في Play Console (الإعلان فقط).
        -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1725525147318224~6939671809" />

        <!-- يؤجّل تهيئة الإعلانات حتى بعد موافقة المستخدم (UMP) -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      معرّف الإعلانات (Advertising ID) — إلزامي لعمل AdMob على Android 13+ (API 33+).
      ملاحظة: حزمة play-services-ads تدمج هذا الإذن تلقائياً، لكن نُصرّح به هنا
      صراحةً للتوثيق والوضوح. في Play Console → "المعرّفات الإعلانية" يجب
      اختيار "نعم" — لأن التطبيق يعرض إعلانات AdMob فعلاً.
      بدونه: يُرجع المعرّف أصفاراً وتتعطّل الإعلانات المخصّصة.
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />


    <!-- دعم كل أحجام الشاشات: من أصغر هاتف إلى أكبر تابلت/TV -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
        android:resizeable="true" />

</manifest>
