{
  "name": "decipher-tahiro",
  "version": "5.0.0",
  "description": "Decipher Tahiro — لعبة ألغاز عربية ذكية",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "copy": "cap copy android",
    "open:android": "cap open android",
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease",
    "bundle:android": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/share": "^6.0.3",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}